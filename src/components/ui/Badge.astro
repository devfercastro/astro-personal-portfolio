---

interface Props {
	text: string;
	icon?: string;
}

const { text, icon } = Astro.props;
---
<div class="flex items-center justify-center rounded-xl bg-[var(--badge-bg)] px-5 py-1 text-sm font-medium">
	{icon && <img src={icon} alt={text} class="w-5 h-5" />}
	{text}
</div>
